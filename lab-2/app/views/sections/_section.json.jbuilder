json.extract! section, :id, :sectionNumber, :belongs_to, :instructor, :instructorEmail, :term, :buildingAndRoom, :numGradersNeeded, :created_at, :updated_at
json.url section_url(section, format: :json)
